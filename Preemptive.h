#include <stdio.h>
#include "Gantt.h"
#include "functions.h"
Gantt create_preemptive(Process *processes[5]){
    bubble_sort(processes);
    int i;
    for(i = 0;i<5;i++){
        Process *currProcess = processes[i];
        
    }
}
